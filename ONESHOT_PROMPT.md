You are working in C:\dev\claude-hackathon\shorecheck (Next.js 16.3.5, React 19, Tailwind 4, react-leaflet 5). Read CLAUDE.md and BUILDPLAN.md in this folder first and follow BUILDPLAN.md exactly; it is the whole spec. All research is done: every fact, URL, threshold, number and gotcha you need is in BUILDPLAN.md (section "Already answered") and ../feasibility/VERDICT.md (section "BUILD HANDOFF"). Do not re-research, re-fetch, re-test or re-derive anything; do not call the Copernicus API; do not open the feasibility Python scripts except to copy a URL. If something is unclear, make the simplest assumption and state it in your summary. Do not add anything not in it. Hard time budget: 30 minutes. Work in this order and do not stop between steps:

1. `npm install`. Read `node_modules/next/dist/docs/` for App Router route handlers before writing any route (AGENTS.md requires it).
2. Create `src/lib/lakes.ts` (types + loader for `/data/lakes.json`, `daysSince`, `clearLooks30d`).
3. Create `src/app/api/weather/route.ts` and `src/app/api/sightings/route.ts` exactly as specified in BUILDPLAN.md Step 2 (URLs, fields, thresholds, cache). Weather is regional, one station, labelled as such.
4. Edit `src/components/MapView.tsx`: lake polygons from `public/data/lakes.json`, "Lakes" filter chip, `LakeCard` popup with the four timestamped rows (bloom conditions, reported sightings, official testing, satellite) and the footer "Satellite index unvalidated. Not a safety rating." Keep all existing beach and event behaviour.
5. Edit `src/components/PhoneApp.tsx`: add the Bloom conditions strip above "Happening nearby" fed by `/api/weather`, refreshing every 10 minutes; append the intro sentence. Do not touch HugoChat.
5b. Teammate's project link (2 minutes, no more): add an external link "Which Tree Falls First ↗" pointing to https://halifax-tree-screening.fly.dev/ (target="_blank", rel="noopener noreferrer") in two places: as a third entry in LINKS in `src/components/Nav.tsx` (both GlassNav and Nav render it; use a plain <a> for the external href, not next/link), and as a secondary glass pill button under the headline copy in `src/app/page.tsx` with the one-line blurb "Ranks HRM's tree-hazard queue so crews go to the branch that will actually hit a house." Do not fetch, iframe, embed, proxy, test or modify that app; it is a link only.
6. `npm run dev`, open http://localhost:3000/#app, verify: strip shows a label and today's time; Sandy Lake popup shows four rows; Lake Echo shows last clear look 2026-09-11; Oat Hill shows "too small for satellite". Run `npm run lint` and fix errors.

7. Design pass, only after step 6 passes and only with the time left: the phone must feel like a real iPhone app on an Apple product page. Apple rules: one accent colour (keep sky-700), generous whitespace, 8-pt spacing grid, large thin headline type and small tight labels using the local PP Neue Gstaad / PP Fraktion Mono fonts already in layout.tsx, translucent glass bars (backdrop-blur, white/60), 22-px card radii, hairline separators (border-slate-200/60), soft single shadow, no borders on cards, no emoji, no more than three type sizes per screen, animations 200 ms ease-out only. Use shadcn/ui for the new pieces only: `npx shadcn@latest init -d` then `npx shadcn@latest add card badge separator skeleton`; build LakeCard rows with Card + Separator, the labels with Badge (outline variant), loading with Skeleton. Do not restyle HugoChat or the landing hero, do not change the PhoneFrame geometry, do not add a component library beyond shadcn. If shadcn init fails in under 2 minutes of trying, skip it and use plain Tailwind with the same rules.

Rules: never interpolate satellite values between passes; every signal shows its own timestamp; never use the words current, real-time, safe, unsafe, toxic, bloom detected, pets, dogs. Credentials are not needed for anything in this build. If the province page cannot be parsed, return parsed:false and let the UI say "list checked HH:MM, could not parse". Finish with a 5-line summary of what works and what you skipped.
